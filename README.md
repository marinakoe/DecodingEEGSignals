# Decoding_EEG_Signals
MATLAB code for neural decoding of EEG signals using linear Support Vector Machines (SVM). Includes binary classification, time-resolved decoding, electrode-region comparisons, and analyses with and without pseudo-averaging.
